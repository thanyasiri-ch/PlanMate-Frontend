<template>
  <div>
    <h2>Add Topics and Assignments</h2>
    <!-- Manage topics + assignments for each course -->
    <button @click="$emit('back')">Back</button>
    <button @click="$emit('next')">Next</button>
  </div>
</template>
